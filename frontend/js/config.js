/**
 * ScholarChain — Contract Configuration
 * Update CONTRACT_ADDRESS after deploying with `npx hardhat run scripts/deploy.js`
 */
const CONFIG = {
  // Default contract address (update after deployment)
  CONTRACT_ADDRESS: "",
  // ABI is loaded from abi.json in the same directory
};

export default CONFIG;
